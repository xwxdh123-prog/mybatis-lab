<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.mapper.EmpMapper">
    <resultMap id="empResultMap" type="Emp">
        <id property="empId" column="emp_id"/>
        <result property="empName" column="emp_name"/>
        <result property="gender" column="gender"/>
        <result property="dept" column="dept"/>
        <result property="post" column="post"/>
        <result property="salary" column="salary"/>
        <result property="hireDate" column="hire_date"/>
        <result property="status" column="status"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="empColumns">
        emp_id, emp_name, gender, dept, post, salary,
        hire_date, status, email, phone, created_at
    </sql>

    <select id="selectAllCustom" resultMap="empResultMap">
        SELECT <include refid="empColumns"/>
        FROM emp
        ORDER BY emp_id
    </select>

    <select id="selectByIdCustom" parameterType="int" resultMap="empResultMap">
        SELECT <include refid="empColumns"/>
        FROM emp
        WHERE emp_id = #{empId}
    </select>

    <insert id="insertCustom" parameterType="Emp"
            useGeneratedKeys="true" keyProperty="empId" keyColumn="emp_id">
        INSERT INTO emp (
            emp_name, gender, dept, post, salary,
            hire_date, status, email, phone
        )
        VALUES (
            #{empName}, #{gender}, #{dept}, #{post}, #{salary},
            #{hireDate}, #{status}, #{email}, #{phone}
        )
    </insert>

    <update id="updateCustom" parameterType="Emp">
        UPDATE emp
        SET emp_name = #{empName},
            gender = #{gender},
            dept = #{dept},
            post = #{post},
            salary = #{salary},
            hire_date = #{hireDate},
            status = #{status},
            email = #{email},
            phone = #{phone}
        WHERE emp_id = #{empId}
    </update>

    <delete id="deleteCustomById" parameterType="int">
        DELETE FROM emp
        WHERE emp_id = #{empId}
    </delete>

    <select id="selectByCondition" parameterType="Emp" resultMap="empResultMap">
        SELECT <include refid="empColumns"/>
        FROM emp
        <where>
            <if test="empName != null and empName != ''">
                AND emp_name LIKE CONCAT('%', #{empName}, '%')
            </if>
            <if test="gender != null and gender != ''">
                AND gender = #{gender}
            </if>
            <if test="dept != null and dept != ''">
                AND dept = #{dept}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY emp_id
    </select>

    <update id="updateDynamic" parameterType="Emp">
        UPDATE emp
        <set>
            <if test="empName != null">emp_name = #{empName},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="dept != null">dept = #{dept},</if>
            <if test="post != null">post = #{post},</if>
            <if test="salary != null">salary = #{salary},</if>
            <if test="hireDate != null">hire_date = #{hireDate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
        </set>
        WHERE emp_id = #{empId}
    </update>

    <insert id="insertBatch">
        INSERT INTO emp (
            emp_name, gender, dept, post, salary,
            hire_date, status, email, phone
        )
        VALUES
        <foreach collection="list" item="employee" separator=",">
            (
                #{employee.empName}, #{employee.gender}, #{employee.dept},
                #{employee.post}, #{employee.salary}, #{employee.hireDate},
                #{employee.status}, #{employee.email}, #{employee.phone}
            )
        </foreach>
    </insert>

    <delete id="deleteBatch">
        DELETE FROM emp
        WHERE emp_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectByPriority" resultMap="empResultMap">
        SELECT <include refid="empColumns"/>
        FROM emp
        <where>
            <choose>
                <when test="empName != null and empName != ''">
                    emp_name LIKE CONCAT('%', #{empName}, '%')
                </when>
                <when test="dept != null and dept != ''">
                    dept = #{dept}
                </when>
                <otherwise>
                    1 = 1
                </otherwise>
            </choose>
        </where>
        ORDER BY emp_id
    </select>
</mapper>
